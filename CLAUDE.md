## Filesystem Search — HARD RULE

**NEVER search at the root of my machine (`/`, `/System`, etc.).** Everything ever related to my work lives under `~/Developer`. Scope all searches there. If you ever think you need to look somewhere else, ask me first.

## Git — HARD RULES (NO EXCEPTIONS)

Every `git commit`, `git push`, `gh pr create`, and any command that writes to the remote is BLOCKED until I explicitly say to do it in that moment. "Open a PR" is approval for that PR only — not for future commits or pushes in the same conversation. Each destructive or remote-writing git operation requires its own explicit approval. Do not infer ongoing permission from a previous approval.

- **NEVER commit without my explicit approval.** Show me the diff and wait.
- **NEVER push without my explicit approval.** Tell me what you want to push and wait.
- **NEVER create PRs without my explicit approval.**
- **NEVER force push.** Only if I explicitly tell you to, and that will be rare. Default to creating new commits instead of amending.
- **NEVER reset or drop databases without my explicit permission.**
- Always run tests and lint before proposing to push.
- When reverting, use surgical operations (`git checkout main -- <file>`, `git revert`) — NEVER hard-reset entire branches.
- NEVER add `Co-Authored-By:` trailers. Write a semantically correct commit message only.

If in doubt, ask. The cost of asking is zero. The cost of an unauthorized push is my trust.

## Workflow

- Reuse existing helpers, utilities, and patterns — search the codebase (shared utils, test helpers, component patterns) before writing new code.
- Prefer the simplest approach first.
- Never add explanatory comments; rationale belongs in the commit or PR.

## Pull Requests

Start every PR description with a short **TL;DR** (1–2 sentences), then a tight description of the problem and how it was solved, then a minimal test plan. Don't waste reviewer time. Do NOT add self-promotion — I don't want to see any "Generated with Claude Code" references in my pull requests ever.

## Impersonation

**ALWAYS** credit yourself when posting on my behalf (e.g. replying to GitHub comments). Example: "This comment was generated by AI (Claude)"

## Shell Environment

Do not prepend `eval "$(/opt/homebrew/bin/mise activate zsh)"` or similar mise activation to Bash commands. `CLAUDE_ENV_FILE` (`~/.claude/env-setup.sh`) already activates mise with `--shims`, so `gh`, `node`, `pnpm`, etc. are on the PATH.
